hrs = input("Enter Hours:")
h = float(hrs)

rate = input("Enter rate per Hours:")
r = float(rate)

diff = (h-40)

if h <= 40:
    pay = (h * r)
    print(pay)

elif h >= 40:
 rateabove40 = (1.5 * r)
 Pay40 = (h * r)

 #pay = (h * r) +
 print(Pay40)
